#!/bin/bash

# General
sudo apt-get update

# Python
sudo pip install bs4
sudo pip install BeautifulSoup
sudo apt-get install graphviz
sudo pip install graphviz
sudo pip install pydot

# R
echo "Please manually install the version of R and RStudio appropriate for your system requirements (Penn's workstations should have both already installed)"

# C++14
sudo apt-get install build-essential
sudo apt-get install g++-4.9
